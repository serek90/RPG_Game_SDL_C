on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

image: gcc

jobs:
    build:
        runs-on: linux-latest

    steps:
        - cd RPG_LOR_Game
        - apt-get update 
        - apt-get -y install cmake
        - mkdir build
        - cd build
        - cmake ..
        - make
